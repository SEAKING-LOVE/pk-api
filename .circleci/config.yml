version: 2
jobs:
  build:
    working_directory: ~/pk-api
    docker:
      - image: circleci/node:8.7.0
    steps:
      - checkout
      - restore_cache:
          key: dependency-cache-{{ checksum "package.json" }}
      - run:
          name: Install node modules
          command: npm install
      - save_cache:
          key: dependency-cache-{{ checksum "package.json" }}
          paths:
            - node_modules
  test:
    working_directory: ~/pk-api
    docker:
      - image: circleci/node:8.7.0
    steps:
      - checkout
      - run:
          name: Run tests
          command: echo "No tests for now"

  deployment:
    working_directory: ~/pk-api
    docker:
      - image: circleci/node:8.7.0
    steps:
      - deploy:
          name: Deploy to dokku
          command: |
            if [ "${CIRCLE_BRANCH}" == "master" ]; then
              git remote add dokku dokku@$server_ip:pk-api.git
              git push dokku master
            fi

workflows:
  version: 2
  build_test_deploy:
    jobs:
      - build
      - test:
          requires:
            - build
          filters:
            branches:
              only: master
      - deployment:
          requires:
            - build
            - test
          filters:
            branches:
              only: master

